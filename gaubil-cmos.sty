\NeedsTeXFormat{LaTeX2e}[1994/06/01]
\ProvidesPackage{gaubil-cmos}[2021/03/25 GaubilCMOfS]

% ----- Multiple languages ------
\RequirePackage[greek,french,english]{babel}
\RequirePackage[font=footnotesize,labelfont=bf]{caption}
\RequirePackage{multicol}

\RequirePackage{float}
\RequirePackage{booktabs}

% ----- TikZ -----
\RequirePackage{tikz}
\usetikzlibrary{tikzmark,arrows,calc}

\RequirePackage{graphicx}
\graphicspath{ {/Users/alexandregaubil/Desktop/} }
\newcommand{\addimage}[2]{
    \begin{center}
        \includegraphics[#2]{#1}
    \end{center}
}

\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\RequirePackage{listings}
\lstdefinelanguage{pseudocode}{
    keywords = {for,procedure,else,if,return,then,do}
}
\lstset{
  basicstyle=\ttfamily,
  mathescape,
  numbers=left,
  stepnumber=1,
  breaklines=true,
  breakatwhitespace=false,
  showstringspaces=false,
  tabsize=1,
  language=pseudocode,
  escapechar=|,
  xleftmargin=5.0ex,
  numberstyle=\small\fontfamily{cmtt}\selectfont,
}

% load textcomp to get rid of micro and perthousand warning
\RequirePackage{textcomp, gensymb, multirow}
\RequirePackage{enumitem}

\RequirePackage[hidelinks]{hyperref}

% ----- General packages ------
\RequirePackage{setspace, fullpage, enumitem, tabularx, lmodern, xcolor}

% To avoid reruning latexmk many times
\RequirePackage{bookmark}

\RequirePackage{array}
\newcolumntype{P}[1]{>{\centering\arraybackslash}p{#1}}

% ----- Multiple languages ------
\RequirePackage[T1]{fontenc}
\newcommand{\textfrench}[1]{\foreignlanguage{french}{#1}}




% ----- Math -----
\RequirePackage{amsmath, amsfonts}




% ----- Physics -----
\RequirePackage{siunitx}

\sisetup{inter-unit-product =$\cdot$}
\newcommand{\Epsilon}{\mathcal{E}}
\newcommand{\td}{\text{d}}
\newcommand{\Pw}{\mathscr{P}}




% ----- Computer Science -----
\RequirePackage{fancyvrb} % Provides Verbatim environment




% GENERAL --- Formatting paper to follow CMOfS
%\doublespacing
\setlength{\parindent}{4em}

\newcommand{\studentid}{\# 12240066}
\newcommand{\studentname}{Alexandre N.~Gaubil}
\newcommand{\wordcount}[1]{\begin{flushright} {\it(#1 words)} \end{flushright}}

\newenvironment{proofenum}{
    \begin{proof}
        \hfill
        \begin{enumerate}[label={(\alph*)}]
}{
    \qedhere
        \end{enumerate}
    \end{proof}
}


%\RequirePackage{csquotes}
%\RequirePackage{cite}
%\RequirePackage[maxcitenames=2]{biblatex}
%\RequirePackage{biblatex-chicago}
%\RequirePackage{chicago}
%\RequirePackage[notes,backend=biber]{biblatex-chicago}
%\RequirePackage{natbib}
%\addto{\captionsenglish}{\renewcommand{\refname}{\newpage \rm References}}
%\addto{\captionsenglish}{\renewcommand{\refname}{\newpage}}
%\newcommand{\addbibliography}[1]{\newpage \bibliographystyle{chicago} \bibliography{#1}}

\def\showdraftc{1}
\newcommand{\draftcomment}[1]{\if\showdraftc1\textcolor{gray}{{\sl #1}}\else{}\fi}
\newcommand{\draftchange}[1]{\if\showdraftc1\textcolor{red}{#1}\else#1\fi}

\newcommand{\coursename}{FIXME -- Provide course name}

% ----- MLA - Header -----
\newcommand{\authorname}{\textcolor{red}{Please enter author name by `\textbackslash renewcommand\{\textbackslash authorname\}\{{\it author name}\}'}}
\newcommand{\professorname}{\textcolor{red}{Please enter professor name by `\textbackslash renewcommand\{\textbackslash professorname\}\{{\it professor name}\}'}}
\newcommand{\class}{\textcolor{red}{Please enter class name by `\textbackslash renewcommand\{\textbackslash class\}\{{\it class name}\}'}}
\newcommand{\duedate}{\textcolor{red}{Please enter date by `\textbackslash renewcommand\{\textbackslash duedate\}\{{\it date}\}' }}

\newcommand{\mlaheader}{
    {
        \rm

        {\noindent \authorname}

        {\noindent \professorname}

        {\noindent \class}

        {\noindent Due \duedate } \newline
    }
}

\newcommand{\mlaprompt}[1]{
    \rm

    {\noindent Prompt: {\it #1}} \newline
}

\newcommand{\mlatitle}[1]{
    \begin{center}
        \underline{#1}
    \end{center}
}

\endinput